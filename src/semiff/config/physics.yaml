# Warp 物理仿真参数配置
simulation:
  dt: 0.01  # 时间步长
  num_substeps: 10  # 子步数
  num_iterations: 1000  # 仿真迭代次数

# 物理参数
physics:
  gravity: [0.0, 0.0, -9.81]  # 重力加速度
  friction: 0.5  # 摩擦系数
  restitution: 0.2  # 恢复系数

# 优化参数 (系统辨识)
optimization:
  lr: 0.001
  num_epochs: 100
  loss_threshold: 0.01
